import { Injectable } from "@nestjs/common";


type OpenWhiteboardUsecaseProps = {

};

@Injectable()
export class OpenWhiteboardUsecase {

  constructor({

  } : OpenWhiteboardUsecaseProps) {}

  async execute() {

    // 1. 방에서 보낸것이 맞는지 확인 

    // 2. 서명화

    // 3. 반환

  }
};